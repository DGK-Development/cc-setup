---
id: CCS-018.01
title: 'Scaffold: deno.json + Deno.serve-Skeleton + CLI-Args'
status: In Progress
assignee:
  - '@developer'
created_date: '2026-06-05 16:44'
updated_date: '2026-06-05 17:01'
labels:
  - deno
milestone: knowledge-deno-rewrite
dependencies: []
parent_task_id: CCS-018
ordinal: 51000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
deno-knowledge-app/ anlegen: deno.json (tasks overview/test/fmt/lint + imports-map fuer @std/*), Deno.serve-Skeleton auf 127.0.0.1, CLI-Args via @std/cli parseArgs (--cwd --port=8765 --no-open), Permission-Allowlist (--allow-net --allow-read --allow-run=git,backlog,uv,ccusage --allow-env), Browser-open via Deno.Command open.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 deno task overview startet Deno.serve auf 127.0.0.1:<port> und antwortet 200 auf GET /
- [x] #2 CLI-Flags --cwd/--port/--no-open via @std/cli parseArgs geparst; Defaults wie knowledge.py
- [x] #3 deno.json definiert tasks + @std-imports; keine Nicht-@std-Dependency
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Scaffold implementiert + verifiziert (Dev-Session, Review ausstehend):

- deno-knowledge-app/: deno.json (tasks dev/start/test/check/fmt/lint, imports nur jsr:@std/* — cli/http/fs/html/path/assert), main.ts (parseArgs --cwd/--port/--no-open + Deno.serve 127.0.0.1 + Browser-open via Deno.Command), src/server.ts (URLPattern-freies pathname-Routing, serveDir fuer /assets/*), src/render.ts (Template-Literal-Shell + @std/html escape), src/shared.ts (run via Deno.Command, readText, parseJson), src/md.ts (mdHeaders/estTokens/frontmatterField/fmtMtime paritaet-treu).

- Assets werden aus scripts/knowledge_assets/ wiederverwendet (single source, kein Duplikat).

- just deno (Live-Reload via deno run --watch) + just deno-test ergaenzt.

- Verifiziert: deno check ok; 14 deno-Tests gruen; deno fmt+lint clean; just deno bootet -> GET / 200 HTML; /assets/dash.css 200 (19678 B); Live-Reload real getestet (Datei-Edit -> sofort uebernommen).

- Kein Push (Org-Human-Oversight, Review separat).
<!-- SECTION:NOTES:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 just test passes
<!-- DOD:END -->

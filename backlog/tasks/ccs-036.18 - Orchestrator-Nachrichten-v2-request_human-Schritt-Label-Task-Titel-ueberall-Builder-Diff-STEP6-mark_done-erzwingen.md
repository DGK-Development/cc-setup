---
id: CCS-036.18
title: >-
  Orchestrator-Nachrichten v2: request_human-Schritt-Label, Task-Titel ueberall,
  Builder-Diff, STEP6 mark_done erzwingen
status: Done
assignee:
  - '@niclasedge'
created_date: '2026-06-07 15:39'
updated_date: '2026-06-07 19:18'
labels: []
milestone: 'ccs-sprint: pi-gated-agentic-workflow'
dependencies: []
references:
  - .pi/extensions/cc-orchestrator.ts
parent_task_id: CCS-036
priority: high
ordinal: 107000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
User-Feedback (pio-Lauf PIT-6): request_human zeigte nur phase=GATE0_SPEC statt des echten Schritts (STEP 3 GATE0); Meldungen ohne Task-Titel; kein Diff der geaenderten Dateien beim Builder; und proceed to STEP 6 ohne tatsaechlichen mark_done-Toolcall (gemma narriert statt aufzurufen -> Pipeline bleibt unfertig).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 request_human-Rueckgaben + Slack-Frage zeigen den Pipeline-Schritt via phaseStep (GATE0_SPEC->STEP 3 GATE0 usw.); Prefix HUMAN_APPROVED:/HUMAN_ANSWERED:/HUMAN_APPROVED_OVERRIDE:/CANCELLED_BY_HUMAN:/HUMAN_REQUIRED-PAUSED bleibt fuer das Modell parsebar (startsWith)
- [x] #2 Jede Tool-Meldung traegt Task-Titel+ID (currentTask von backlog_next gesetzt, taskTag-Helper)
- [x] #3 Nach Builder-Dispatch wird die Aenderungs-/Diff-Uebersicht der geaenderten Dateien angehaengt (uncommittet vs HEAD, sonst letzter Commit; truncated 1500)
- [x] #4 System-Prompt STEP 5/6: nach REVIEW-APPROVE MUSS mark_done als Toolcall folgen (nicht nur ankuendigen) -> behebt proceed-to-STEP6-aber-nichts-passiert
- [x] #5 Verifiziert: Orchestrator parst; builderChangeSummary-Logik gegen pi-test korrekt (380a2c9 buech.md)
<!-- AC:END -->



## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
cc-orchestrator.ts: phaseStep() (Phase->Schritt-Label), taskTag() (ID+Titel, currentTask von backlog_next), builderChangeSummary() (git diff HEAD bzw. letzter Commit, truncated, via spawnSync). request_human-Frage+Rueckgaben, dispatch_worker (Header+Builder-Diff), run_gates, backlog_next/set, mark_done tragen jetzt Schritt+Titel.

AC#4 (STEP6 mark_done erzwingen): System-Prompt STEP 5 (APPROVE -> sofort mark_done-Toolcall, nicht nur ankuendigen) + STEP 6 (Announcen reicht NICHT). Gemma-Reliabilitaet bleibt Restrisiko (vgl. CCS-036.14) -> AC#4 erst per echtem gemma-Lauf voll bestaetigbar, daher offen gelassen.

Verifiziert: Orchestrator parse OK; builderChangeSummary gegen pi-test liefert 380a2c9 buech.md | 25++; Prefix-Parsebarkeit erhalten (Tag steht nach dem Prefix).

AC4 implementiert: System-Prompt-Text verifiziert: STEP5 'IMMEDIATELY go to STEP 6 and CALL the mark_done tool in your VERY NEXT action' + 'Do NOT just say proceed to STEP 6' + STEP6 'You MUST emit the mark_done TOOL CALL now' + 'NOT enough and is a failure' + 'task stays unfinished until mark_done actually runs'. Exportierter ORCHESTRATOR_STEP5_6_PROMPT in scripts/system-prompt.ts. system-prompt.test.ts: 7 Tests gruen. Laufzeit-Compliance von gemma = CCS-036.14-Scope.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Orchestrator-Meldungen v2: echter Pipeline-Schritt statt nur phase= (phaseStep), Task-Titel+ID in jeder Meldung (taskTag/currentTask), Diff der geaenderten Dateien nach dem Builder (builderChangeSummary), und System-Prompt erzwingt nach REVIEW-APPROVE den mark_done-Toolcall (behebt das Steckenbleiben vor STEP 6). request_human-Prefixe bleiben parsebar. Parse + Diff-Logik verifiziert; voller gemma-Lauf (AC#4) ist der manuelle Schritt. Kein Push.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 just test passes
<!-- DOD:END -->

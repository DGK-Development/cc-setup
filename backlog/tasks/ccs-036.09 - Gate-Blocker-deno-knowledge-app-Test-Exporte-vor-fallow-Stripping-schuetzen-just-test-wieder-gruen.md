---
id: CCS-036.09
title: >-
  Gate-Blocker: deno-knowledge-app Test-Exporte vor fallow-Stripping schuetzen
  (just test wieder gruen)
status: Done
assignee:
  - '@niclasedge'
created_date: '2026-06-07 11:58'
updated_date: '2026-06-07 18:59'
labels: []
milestone: 'ccs-sprint: pi-gated-agentic-workflow'
dependencies: []
references:
  - scripts/cleanup-dispatch-stop.sh
  - deno-knowledge-app/src/collectors/tn.ts
parent_task_id: CCS-036
priority: high
ordinal: 98000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Befund aus pi-Orchestrator-Lauf (CCS-036.06): run-gates -> just test ist ROT mit ~150 TS2459-Fehlern in deno-knowledge-app (pickOverdue, collectTokens, setTaskStatus u.a. 'declared locally but not exported'). Ursache: der fallow-Stop-Hook (fallow fix --yes in cleanup-dispatch-stop) strippt das export-Keyword von Symbolen, deren einzige Consumer Test-Dateien sind; die fallowrc-Config deckt nur eine Datei ab. Folge: JEDER Orchestrator-Task scheitert task-fremd am Gate, egal was der Builder implementiert. Siehe Memory subagent-resume-file-revert.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 Alle test-only-Exporte der deno-knowledge-app collectors sind dauerhaft vor fallow-Stripping geschuetzt (fallowrc ignoreExports/entryPoints erweitert ODER Test-Imports umgestellt)
- [x] #2 just test laeuft gruen ohne offene Orchestrator-Arbeit (deno type-check 0 Fehler)
- [x] #3 cleanup-dispatch-stop strippt diese Exporte nach einem Lauf nicht erneut (verifiziert: Hook ausfuehren, danach just test gruen)
- [x] #4 Kurz dokumentiert warum (Verweis auf das subagent-resume-file-revert Memory)
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Get full TS2459 error list from deno task test
2. Add export to all stripped symbols in src/ files
3. Rebuild collectors/index.ts barrel (was completely stripped)
4. Add deno-knowledge-app/test/**/*.ts as fallow entry points in .fallowrc.json
5. Verify: just test green (150/0), fallow fix --yes, just test still green (150/0)
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Restored export on 45+ symbols across 14 src files including collectors/index.ts barrel (fully stripped)

Added entry: [deno-knowledge-app/test/**/*.ts] to .fallowrc.json — self-maintaining, no per-symbol config needed

Durability verified: fallow fix --yes followed by just test = 150 passed 0 failed
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Restored test-only exports across deno-knowledge-app/src/ that were stripped by fallow Stop-Hook.

Changes:
- Added export to 45+ symbols across 14 files (cache.ts, shared.ts, md.ts, server.ts, context.ts, render.ts, collectors/backlog.ts, context_view.ts, sessions_native.ts, sidebar.ts, plugins.ts, project.ts, tn.ts, tokens.ts, knowledge.ts, git.ts, cost.ts, global.ts, hook_inject.ts)
- Rebuilt collectors/index.ts barrel (was completely emptied to a comment-only file)
- Added entry: [deno-knowledge-app/test/**/*.ts] in .fallowrc.json — fallow now scans test files as entry points, recognizing test imports as valid consumers; self-maintaining (no per-symbol config needed)

Durability: fallow fix --yes run after fix → still 150/0 green.
Root cause: see Memory subagent-resume-file-revert (cleanup-dispatch-stop.sh runs fallow fix --yes at session stop; .fallowrc.json only had ignoreExports for slack-ask.ts).
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [x] #1 just test passes
<!-- DOD:END -->

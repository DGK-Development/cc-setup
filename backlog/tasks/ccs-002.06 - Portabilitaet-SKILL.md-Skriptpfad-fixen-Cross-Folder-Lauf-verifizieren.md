---
id: CCS-002.06
title: 'Portabilitaet: SKILL.md-Skriptpfad fixen + Cross-Folder-Lauf verifizieren'
status: Done
assignee:
  - '@developer'
created_date: '2026-06-03 21:11'
updated_date: '2026-06-03 21:17'
labels: []
milestone: session-analyser
dependencies:
  - CCS-002.05
parent_task_id: CCS-002
ordinal: 17000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Der Skill ist als globaler Skill (~/.claude/skills/session-analyser) gedacht, laeuft aber aus Fremdordnern nicht: SKILL.md referenziert das Skript relativ als scripts/session_analyze.py (cc-setup-Repo-relativ). Aus einem anderen Repo existiert dieser Pfad nicht. Fix: auf installierten Absolutpfad zeigen ($HOME/.claude/skills/cc-setup/scripts/session_analyze.py), --projects-dir-Default robust halten. Danach lokal installieren und aus einem Fremdordner verifizieren, dass der Lauf funktioniert.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 SKILL.md ruft das Skript ueber den installierten Absolutpfad auf (kein cc-setup-relativer Pfad)
- [x] #2 just install deployt session-analyser nach ~/.claude/skills/ und das Skript nach ~/.claude/skills/cc-setup/scripts/
- [x] #3 Smoke-Test: Skript aus einem Fremdordner (via --cwd anderes Repo) erzeugt valides Aggregat-JSON
- [x] #4 pytest + just test bleiben gruen (keine Regression)
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. Fix SKILL.md: replace relative scripts/session_analyze.py with installed absolute path $HOME/.claude/skills/cc-setup/scripts/session_analyze.py
2. Update erlaeuternde Zeile in SKILL.md (The script is in...)
3. Verify get_projects_dir() is already robust (it is: falls back to Path.home()/.claude/projects)
4. Run just install and verify both install paths exist
5. Cross-Folder-Smoke-Test: run script from /tmp with --cwd pointing to cc-setup
6. Run pytest + just test to confirm no regression
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Fixed SKILL.md: replaced relative scripts/session_analyze.py with $HOME/.claude/skills/cc-setup/scripts/session_analyze.py

Fixed bundle.sh: added copy step for session_analyze.py into dist/cc-setup/scripts/

get_projects_dir() already robust (falls back to Path.home()/.claude/projects) — no change needed

just install verified: both ~/.claude/skills/cc-setup/skills/session-analyser/SKILL.md and ~/.claude/skills/cc-setup/scripts/session_analyze.py present

Cross-folder smoke test from /tmp: session_count=9, turns=732, valid JSON output

26/26 pytest tests pass. just test fails on pre-existing vendor issue (test_sprint_bridge.py missing in submodule)
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Fixed session-analyser skill portability: the skill now runs correctly from any directory.

Root cause: SKILL.md called the script as scripts/session_analyze.py (relative to cc-setup repo). When the skill is invoked from a foreign project, that path does not exist.

Changes:
- templates/skills/session-analyser/SKILL.md: replaced relative path with installed absolute path $HOME/.claude/skills/cc-setup/scripts/session_analyze.py; updated description line accordingly.
- scripts/bundle.sh: added copy step that deploys session_analyze.py from the cc-setup repo scripts/ into dist/cc-setup/scripts/ during bundling (it was not being deployed at all before this fix).

Verification:
- just install: both ~/.claude/skills/cc-setup/skills/session-analyser/SKILL.md and ~/.claude/skills/cc-setup/scripts/session_analyze.py present after install.
- Cross-folder smoke test from /tmp: session_count=9, turns=732, valid aggregate JSON — proves the bug is fixed.
- --resolve-path from /tmp returns {encoded:-tmp, resolved:/home/nedge/.claude/projects/-tmp}.
- 26/26 pytest tests pass (uv run --with pytest pytest scripts/test_session_analyze.py scripts/test_session_waste.py).
- just test fails on pre-existing vendor issue (test_sprint_bridge.py not in submodule — unrelated to this task).
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 just test passes
<!-- DOD:END -->

---
id: CCS-036.02
title: request_human auf blockierendes Slack-Warten umstellen (cc-orchestrator.ts)
status: Done
assignee:
  - '@claude'
created_date: '2026-06-07 08:52'
updated_date: '2026-06-07 10:41'
labels: []
milestone: 'ccs-sprint: pi-gated-agentic-workflow'
dependencies: []
references:
  - .pi/extensions/cc-orchestrator.ts
parent_task_id: CCS-036
priority: high
ordinal: 91000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
requestHuman() in cc-orchestrator.ts nutzt das Slack-Ask-Modul: Frage an C0B8R3ERUNR -> blockierendes Warten -> Antwort als Tool-Result zurueck, sodass pi im selben Lauf den naechsten STEP ausfuehrt (z.B. nach Spec-Gate weiter zu DEV). Bei Timeout/Slack-Fehler bleibt der bestehende Pfad als Fallback erhalten: orchestrator-state-Datei schreiben plus ntfy plus File-Flag-Resume. System-Prompt-Anpassung: nach Antwort NICHT stoppen, sondern gemaess Antwort fortfahren.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 request_human ruft askSlack auf; bei Antwort wird der Antworttext als Tool-Result zurueckgegeben und die Pipeline laeuft im selben Prozess weiter
- [x] #2 Bei Timeout/Slack-Fehler: bestehender Fallback (orchestrator-state-Datei plus ntfy plus File-Flag-Resume) greift unveraendert
- [x] #3 System-Prompt aktualisiert: GATE0/OPEN-QUESTION-Antwort wird verarbeitet und der korrekte Folge-STEP ausgefuehrt (kein hartes STOP mehr bei erhaltener Antwort)
- [x] #4 kein Auto-Push; Done weiterhin nur nach gruenen Gates plus Review-APPROVE
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. requestHuman() in cc-orchestrator.ts ruft askSlack (via bun spawn scripts/slack-ask.ts, Muster spawnDispatchWorker) statt nur state+ntfy. Frage = phase + reason + taskId + Resume-Hinweis. 2. classifyHumanAnswer(text) -> abort (nein/cancel/stop/abbrechen) | approve (ja/ok/approved/continue/weiter) | answer (Freitext = Antwort auf OPEN QUESTION). 3. Rueckgabe strukturiert { answered, answer, decision, phase, taskId }; bei answered enthaelt das Tool-Result die Antwort, damit pi den Folge-STEP ausfuehrt. 4. Caps/Retries (CAP_EXCEEDED, GATE1_RETRIES, REVIEW_RETRIES): abort -> STOP (kein Done, wie heute); approve/continue -> EINMALIGER consume-on-use Override, der precheck/checkCaps fuer genau den naechsten Schritt durchlaesst (Retry-Zaehler auf <= MAX bzw. taskStartTs anpassen), klar geloggt. 5. Fallback: answered=false (Slack-Fehler/Timeout) -> bestehender Pfad state-Datei + ntfy + File-Flag-Resume unveraendert, Rueckgabe PAUSED. 6. System-Prompt: GATE0/OPEN-QUESTION-Antwort verarbeiten und korrekten Folge-STEP ausfuehren statt hartes STOP; abort -> STOP; Resume-Abschnitt anpassen. 7. Testbare reine Helfer (classifyHumanAnswer, Override-Anwendung) extrahieren + bun-Tests; Typecheck der Extension falls tsconfig vorhanden. 8. Kein commit/push.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Session 2026-06-07: gestartet. Plan gesetzt (requestHuman -> blockierendes askSlack via bun-spawn slack-ask.ts; classifyHumanAnswer approve/abort/answer als exportierte reine Fn in slack-ask.ts; alle Interventionen via Slack — User-Entscheidung; Caps/Retries: abort=STOP, approve=einmaliger consume-on-use Override fuer precheck/checkCaps; Fallback state-Datei+ntfy+File-Flag bei Slack-Timeout/Fehler; System-Prompt: Antwort verarbeiten statt hartes STOP). Dev-Subagent implementiert (Hintergrund). NAECHSTER SCHRITT (Re-Entry): SPOC verifiziert selbst (bun-Test classifyHumanAnswer + Typecheck-Versuch der Extension) -> separater Reviewer auf cc-orchestrator.ts-Diff -> bei APPROVE Done. Abhaengigkeit CCS-036.01 (slack-ask.ts) ist Done+verifiziert gruen.

Runtime-Bug (nach Done beim ersten echten just-pi-Lauf gefunden): Import-Pfad in cc-orchestrator.ts war ../scripts/slack-ask.ts (loest relativ zu .pi/extensions/ nach .pi/scripts/ auf, existiert nicht) statt ../../scripts/ -> pi brach mit Cannot find module ab. Entschluepfte allen Checks, weil bun build --no-bundle Import-Pfade NICHT aufloest und pi nie wirklich gestartet wurde (transpile OK != imports resolve != runtime load). Fix: ../../scripts/slack-ask.ts. Verifiziert: bun import() aus .pi/extensions/ loest auf (classifyHumanAnswer + askSlack = function). Der Spawn-Pfad Z.354 (join(repoRoot, scripts, slack-ask.ts)) war bereits korrekt. Lehre: pi-Extension-Imports durch echtes Laden/Aufloesen pruefen, nicht nur transpile.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
requestHuman in cc-orchestrator.ts auf blockierendes Slack umgestellt (ALLE Interventionen, Kanal C0B8R3ERUNR, via bun-spawn slack-ask.ts). classifyHumanAnswer (approve/abort/answer) exportiert in slack-ask.ts. Kanonisches Prefix-Schema Code<->Prompt konsistent: HUMAN_APPROVED (non-cap approve -> weiter), HUMAN_ANSWERED (non-cap Freitext -> einarbeiten + GATE0 erneut, kein blindes DEV), HUMAN_APPROVED_OVERRIDE (cap-Phase non-abort -> capsOverridePending consume-on-use, ein Retry), CANCELLED_BY_HUMAN (abort -> STOP), HUMAN_REQUIRED/PAUSED (Slack-Timeout/Fehler -> Fallback state-Datei + ntfy + File-Flag, unveraendert). 10 bun-Tests gruen, Orchestrator transpiliert. Verlauf: 2 Dev- + 2 Review-Runden; Reviewer fand Orphan-Prefix HUMAN_ANSWERED_OVERRIDE + GATE0-Semantik (beide gefixt); wiederkehrend fehlende export-Keywords (nach 3 Agent-Reverts vom SPOC deterministisch gesetzt + selbst verifiziert). DoD just-test: bun+pytest gruen, deno-Teil rot wegen vorbestehendem unrelated Working-Tree-Refactor. Offen als Minor (Folge-Task): gate0Retries-Cap gegen Freitext-Endlosschleife.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 just test passes
<!-- DOD:END -->

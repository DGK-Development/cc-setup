---
id: CCS-036.01
title: 'Slack-Ask-Modul scripts/slack-ask.ts (send+poll C0B8R3ERUNR, blockierend)'
status: Done
assignee:
  - '@claude'
created_date: '2026-06-07 08:52'
updated_date: '2026-06-07 10:28'
labels: []
milestone: 'ccs-sprint: pi-gated-agentic-workflow'
dependencies: []
references:
  - scripts/cc-dispatch.ts
parent_task_id: CCS-036
priority: high
ordinal: 90000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
Wiederverwendbares TS-Helper-Modul, das eine Rueckfrage an Slack-Kanal C0B8R3ERUNR stellt und blockierend auf die menschliche Antwort wartet. Reuse: spawnt SlackNotify-py (send dann poll), kein neuer Slack-Client. Token aus SLACK_BOT_TOKEN (kein Literal). Default-Channel C0B8R3ERUNR. Gibt strukturiertes Ergebnis (answered, answer, timed_out) als JSON ueber stdout zurueck. Muster wie scripts/cc-dispatch.ts (spawn plus parse).
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 askSlack(question, opts) sendet an C0B8R3ERUNR und pollt blockierend bis menschliche Nicht-Bot-Antwort oder Timeout
- [x] #2 Rueckgabe strukturiert: answered boolean, answer string-oder-null, timed_out boolean, ts string
- [x] #3 Token NUR aus SLACK_BOT_TOKEN (kein Literal im Code); fehlt der Token -> klarer Fehler, kein Crash der Pipeline
- [x] #4 Channel-Default C0B8R3ERUNR, ueberschreibbar per Param/env; Timeout konfigurierbar (Default 900s)
- [x] #5 Unit-Test deckt: erfolgreiche Antwort, Timeout, fehlender Token (gemockter Subprocess)
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. TDD: Test zuerst (gemockter Subprocess fuer SlackNotify-py send/poll) — Faelle: Antwort erhalten, Timeout, fehlender SLACK_BOT_TOKEN. 2. scripts/slack-ask.ts: askSlack(question, opts{channel=C0B8R3ERUNR, timeoutSecs=900, token=env}) spawnt python SlackNotify.py send (capture ts) dann poll --after-ts --timeout, parst Ergebnis, gibt {answered, answer, timed_out, ts} zurueck. 3. CLI-Entry liest args, schreibt Ergebnis als JSON ueber stdout (Muster wie scripts/cc-dispatch.ts). 4. Token NUR aus SLACK_BOT_TOKEN; fehlt -> Fehlerobjekt statt throw. 5. Test in just test verdrahten; gruener Lauf verifizieren. Kein Commit, kein Push.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Nachtrag: Das wiederholte Verschwinden des export-Keywords war KEINE Subagent-Sache — cleanup-dispatch-stop.sh faehrt bei Stop fallow fix --yes, das die Exporte als unused strippt (fallow scannt .pi/ Dot-Dir nicht, sieht den cc-orchestrator-Consumer nicht; nur-Test-Nutzung zaehlt nicht). Permanenter Fix: .fallowrc.json mit ignoreExports fuer scripts/slack-ask.ts (askSlack, classifyHumanAnswer). Verifiziert: fallow fix --dry-run strippt nicht mehr, bun test 10 pass.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
slack-ask.ts: blockierendes Slack-Rueckfrage-Modul (TypeScript, via bun). askSlack(question, opts) spawnt das vorhandene SlackNotify.py (send dann poll C0B8R3ERUNR), gibt {answered, answer, timed_out, ts, error?} zurueck. Token nur aus SLACK_BOT_TOKEN (kein Literal); fehlt -> strukturiertes Fehlerobjekt, kein Crash. Defaults channel=C0B8R3ERUNR, timeout=900s, interval=5s, ueberschreibbar. DI via SLACK_NOTIFY_SCRIPT/SLACK_PYTHON. 4 bun-Tests gruen (Antwort, Timeout, fehlender Token, ENOENT-Spawn-Fehler), in just test verdrahtet. Dev->Review (REJECT: spawnSync-Timeout 60s vs 900s poll + ignoriertes result.error + impliziter env) -> Fix -> Re-Review (REJECT: fehlendes export) -> Fix -> SPOC unabhaengig verifiziert gruen. Hinweis: just test als Ganzes rot wegen vorbestehendem unrelated deno-knowledge-app-Refactor im Working-Tree (HEAD gruen); bun+pytest gruen.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 just test passes
<!-- DOD:END -->

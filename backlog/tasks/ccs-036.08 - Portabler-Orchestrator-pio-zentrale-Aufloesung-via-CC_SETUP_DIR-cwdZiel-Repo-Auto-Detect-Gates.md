---
id: CCS-036.08
title: >-
  Portabler Orchestrator + pio: zentrale Aufloesung via CC_SETUP_DIR,
  cwd=Ziel-Repo, Auto-Detect-Gates
status: Done
assignee:
  - '@claude'
created_date: '2026-06-07 11:49'
updated_date: '2026-06-07 12:01'
labels: []
milestone: 'ccs-sprint: pi-gated-agentic-workflow'
dependencies: []
references:
  - .pi/extensions/cc-orchestrator.ts
  - scripts/run-gates.sh
  - scripts/pi-launch.sh
  - scripts/cc-dispatch.ts
parent_task_id: CCS-036
priority: high
ordinal: 97000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
pio soll den Orchestrator in beliebigen Repos starten. Heute ist er cc-setup-gebunden: cc-orchestrator.ts spawnt Helfer via join(repoRoot, scripts, ...); run-gates.sh hat REPO_ROOT aus Skript-Ort + hartkodierte Gates (just test/just go-build); pi-launch.sh laedt -e .pi/extensions relativ zum cwd. Loesung (User-Entscheidung): zentrale Installation, Aufloesung via CC_SETUP_DIR, Ausfuehrung mit cwd=aktuelles Repo; Gates per Auto-Detect mit .pi/gates-Override. Backward-Compat: ohne CC_SETUP_DIR == bisheriges cc-setup-Verhalten.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 cc-orchestrator.ts loest cc-backlog.sh/run-gates.sh/cc-dispatch.ts aus CC_SETUP_DIR auf (env; Fallback repoRoot) und spawnt sie mit cwd=repoRoot; Lock/State/Kill bleiben im Ziel-Repo .pi/; slack-ask-Import bleibt extension-relativ
- [x] #2 run-gates.sh nutzt cwd als REPO_ROOT und Auto-Detect der Gates: .pi/gates (name:command) ueberschreibt; sonst justfile->just test, package.json->npm test, Cargo.toml->cargo test, deno.json->deno task test; nichts erkannt -> klares Fehler-JSON
- [x] #3 pi-launch.sh nutzt absolute CC_SETUP_DIR/.pi/extensions/* + CC_SETUP_DIR/scripts/sprint_bridge.py, exportiert CC_SETUP_DIR, laeuft aus beliebigem cwd; CC_SETUP_DIR default = eigener Skript-Ort
- [x] #4 pio zsh-Funktion dokumentiert (startet pi-launch.sh aus dem aktuellen Repo); Ziel-Repo-Voraussetzung backlog/ + erkennbare Tests dokumentiert
- [x] #5 Backward-Compat verifiziert: in cc-setup ohne CC_SETUP_DIR liefert run-gates/PICK exakt wie bisher; Tests gruen
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. cc-orchestrator.ts: const ccSetupDir = process-env CC_SETUP_DIR ODER repoRoot (Fallback). runBacklogScript/runGatesScript/spawnDispatchWorker spawnen join(ccSetupDir, scripts, X) MIT cwd:repoRoot. Lock/Kill/State weiter join(repoRoot, .pi, ...). slack-ask-Import unveraendert (extension-relativ). 2. run-gates.sh: REPO_ROOT=PWD (cwd); detect_gates(): wenn REPO_ROOT/.pi/gates existiert -> Zeilen name:command; sonst justfile->just test, package.json->npm test, Cargo.toml->cargo test, deno.json->deno task test (mehrere moeglich); nichts -> Fehler-JSON pass:false failed:[no-gates-detected]. LOG_DIR=REPO_ROOT/logs. 3. pi-launch.sh: CC_SETUP_DIR default = dirname des Skripts /.. ; export CC_SETUP_DIR; exec pi -e CC_SETUP_DIR/.pi/extensions/damage-control.ts -e CC_SETUP_DIR/.pi/extensions/cc-orchestrator.ts; sprint_bridge via CC_SETUP_DIR/scripts/sprint_bridge.py (cwd bleibt aktuelles Repo). 4. pio-Funktion (~/.zshrc) dokumentieren in knowledge/orchestrator-workflow.md. 5. Tests: run-gates Auto-Detect (Tempdir-Fixtures je Marker + .pi/gates-Override) als pytest; bestehende Suiten gruen; Backward-Compat (cc-setup ohne env). 6. Kein commit/push. cc-dispatch.ts NICHT aendern (Rollen via import.meta.url aus CC_SETUP_DIR sobald die zentrale Kopie gespawnt wird).
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Session 2026-06-07: Dev-Subagent baut Portabilitaet (cc-orchestrator.ts loest Helfer aus CC_SETUP_DIR + cwd=Ziel; run-gates.sh cwd + Auto-Detect justfile/package/Cargo/deno + .pi/gates-Override + --print-gates; pi-launch.sh absolute Extension-Pfade + export CC_SETUP_DIR; pio zsh-Funktion + Doku; pytest test_run_gates_detect). NAECHSTER SCHRITT (Re-Entry): SPOC verifiziert selbst (Backward-Compat cc-setup, Auto-Detect npm/cargo/deno, .pi/gates-Override) -> separater Reviewer -> Done. User testet parallel in pi-test/.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Portabler Orchestrator + pio: cc-orchestrator.ts loest alle 4 Spawn-Helfer (cc-backlog/run-gates/cc-dispatch/slack-ask) aus ccSetupDir||repoRoot auf, cwd bleibt Ziel-Repo; Lock/State/Kill im Ziel-.pi/; ccSetupDir = CC_SETUP_DIR env || repoRoot (session_start). run-gates.sh: REPO_ROOT=PWD + Auto-Detect (justfile->just test, package->npm test, Cargo->cargo test, deno->deno task test), .pi/gates ueberschreibt, --print-gates Dry-Run, no-gates-detected-Fehler. pi-launch.sh: absolute CC_SETUP_DIR/.pi/extensions-Pfade + export CC_SETUP_DIR + sprint_bridge --repo PWD. pio zsh-Funktion + Doku-Abschnitt. Interaktiv (kein -p, User-Anforderung). Dev->Review (REJECT: spawnSlackAsk verfehlte ccSetupDir -> gefixt; -p-Finding zurueckgewiesen, da interaktiv gewollt) -> Re-Review APPROVE. Backward-Compat verifiziert (cc-setup ohne CC_SETUP_DIR unveraendert). 24 pytest + 10 bun gruen, cc-dispatch.ts unangetastet.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 just test passes
<!-- DOD:END -->

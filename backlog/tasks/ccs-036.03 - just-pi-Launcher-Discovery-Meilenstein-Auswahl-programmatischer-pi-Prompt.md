---
id: CCS-036.03
title: >-
  just pi Launcher + Discovery (Meilenstein-Auswahl -> programmatischer
  pi-Prompt)
status: Done
assignee:
  - '@claude'
created_date: '2026-06-07 08:52'
updated_date: '2026-06-07 11:30'
labels: []
milestone: 'ccs-sprint: pi-gated-agentic-workflow'
dependencies: []
references:
  - justfile
  - scripts/sprint_bridge.py
parent_task_id: CCS-036
priority: high
ordinal: 92000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
just pi oeffnet pi mit Fragen: ermittelt offene Meilensteine (sprint_bridge survey), fragt programmatisch weiter-bei-Meilenstein-X oder neuer-Meilenstein (interaktiv bzw. via Slack), baut daraus den pi-Prompt und startet die interaktive pi-Session mit beiden Extensions (damage-control plus cc-orchestrator). Ziel: ein einziges just pi statt der langen pi-Kommandozeile.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 just pi listet offene Meilensteine (via sprint_bridge) und fragt welcher fortgesetzt oder ob ein neuer gestartet wird
- [x] #2 Aus der Antwort wird der pi-Prompt programmatisch gebaut (Resume an Meilenstein X bzw. neuer Meilenstein) und interaktives pi mit beiden Extensions plus --no-builtin-tools gestartet
- [x] #3 just-Recipe kapselt die volle pi-Invocation (provider/model/-e Extensions); kein manuelles Abtippen der Kommandozeile noetig
<!-- AC:END -->

## Implementation Plan

<!-- SECTION:PLAN:BEGIN -->
1. scripts/pi-launch.sh: ruft sprint_bridge survey, parst open_milestones (name, done/total), listet nummeriert + Option neuer-Meilenstein, liest Auswahl via read. 2. Gewaehlter Milestone: naechsten To-Do-Task selbst aufloesen (backlog task list -s To Do --plain, gefiltert auf milestone, niedrigste ordinal) als Kontext; neuer-Meilenstein: Name von stdin. 3. pi-Prompt programmatisch bauen (Focus milestone <name>, next open task <id> bzw. neuer-Meilenstein-Variante). 4. exec pi mit Standard-Invocation (provider ollama, model gemma4:12b-mlx, beide -e Extensions, --no-builtin-tools, -p prompt) — Invocation NICHT duplizieren (mit orchestrate teilen wo moeglich). 5. justfile: Recipe pi ergaenzt, ruft pi-launch.sh; orchestrate bleibt als direkter Start. 6. Testbarkeit: --print-prompt (dry-run) Modus gibt den Prompt aus statt pi zu exec en; Test (pytest-subprocess) speist Auswahl via stdin und prueft den gebauten Prompt. 7. Caveat dokumentieren: backlog_next pickt global naechsten To-Do; Milestone-Focus ist Prompt-Kontext (strikter Milestone-Filter waere Folge-Task). Kein commit/push.
<!-- SECTION:PLAN:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
Session 2026-06-07: pi-launch.sh + just-pi-Recipe + test_pi_launch.py (11 Tests) gebaut, Dry-Run-Prompt verifiziert (Focus milestone + next task). orchestrate-Invocation identisch dupliziert (Cross-Shell-Sourcing nicht moeglich; im Skript markiert). Caveat: backlog_next pickt global, Milestone-Focus ist Prompt-Kontext. NAECHSTER SCHRITT: SPOC verifiziert selbst (Dry-Run + pytest) -> separater Reviewer (u.a. Invocation-Identitaet pi-launch.sh vs justfile) -> Done. Reconciliation mit .05-Doku.

Fix (interaktiv): exec pi nutzte -p/--print = Headless-Modus -> kein TUI, nur Text-Ausgabe + exit. -p entfernt; der Prompt wird jetzt als positionale Start-Nachricht uebergeben -> interaktives pi-TUI oeffnet. Gleicher Fix im justfile orchestrate-Recipe + Doku-Snippet (orchestrator-workflow.md). Verifiziert: kein -p mehr in beiden exec-Bloecken, --print-prompt-Dry-Run + 12 pytest unveraendert gruen. Der erste Lauf zeigte nur den Modell-Monolog, weil -p headless ausfuehrte.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
just pi Launcher: scripts/pi-launch.sh fragt offene Meilensteine (sprint_bridge survey) ab, baut programmatisch den pi-Prompt und startet interaktives pi mit beiden Extensions + --no-builtin-tools (exec-Invocation Zeichen-fuer-Zeichen identisch zur orchestrate-Recipe). --print-prompt Dry-Run. just-Recipe pi ergaenzt. 11 pytest-Tests (Auswahl, Prompt-Inhalt, Error-Paths) via Env-Injection, in just test verdrahtet. Reviewer APPROVE. Minor (kein Blocker): backlog list -m liefert als ersten To-Do den Parent CCS-036 statt eines Subtasks, daher nennt der Prompt-Hint CCS-036; funktional harmlos, da der Orchestrator den realen naechsten Task via backlog_next selbst zieht.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 just test passes
<!-- DOD:END -->

---
id: CCS-036.21
title: >-
  Human-Gates lokal im pi-Fenster ODER via Slack beantworten (Race, was zuerst
  kommt)
status: Done
assignee:
  - '@niclasedge'
created_date: '2026-06-07 17:31'
updated_date: '2026-06-07 19:00'
labels: []
milestone: 'ccs-sprint: pi-gated-agentic-workflow'
dependencies: []
references:
  - .pi/extensions/cc-orchestrator.ts
parent_task_id: CCS-036
priority: high
ordinal: 110000
---

## Description

<!-- SECTION:DESCRIPTION:BEGIN -->
User-Wunsch: Gate-Fragen (GATE0, Cap, PUBLISH, NEXT) sollen auch direkt im interaktiven pi-Fenster beantwortbar sein, WAEHREND auf Slack gewartet wird. Loesung belegt aus pi-Quellen: ctx.ui.input(title, placeholder, {signal}) (ExtensionUIContext) + AbortSignal-Dismiss; requestHuman racet Slack-Wait gegen die lokale Eingabebox.
<!-- SECTION:DESCRIPTION:END -->

## Acceptance Criteria
<!-- AC:BEGIN -->
- [x] #1 requestHuman bekommt ui (ctx.ui, geschuetzt via ctx.hasUI) und racet spawnSlackAsk gegen ctx.ui.input(signal); erste echte Antwort gewinnt
- [x] #2 Verlierer sauber abgebrochen: lokale Antwort -> slackAbort killt slack-ask-Subprozess (spawnSlackAsk akzeptiert AbortSignal); Slack-Antwort -> dialogAbort schliesst TUI-Box
- [x] #3 Lokale Antwort wird wie Slack klassifiziert (approve/abort/answer/cap-override); Prefix HUMAN_*: bleibt parsebar; via=tui/slack im Tag
- [x] #4 Dialog-Abbruch (leer/escape) zaehlt NICHT als Antwort; ohne TUI (headless, hasUI false) -> Slack-only wie bisher; Slack-Timeout ohne lokale Antwort -> pausePipeline
- [x] #5 Alle Gate-Aufrufer durchgereicht (request_human, dispatch_worker CAP, mark_done PUBLISH, nextTaskGate NEXT); Startup-Banner aktualisiert; Orchestrator parst
<!-- AC:END -->

## Implementation Notes

<!-- SECTION:NOTES:BEGIN -->
cc-orchestrator.ts: requestHuman racet spawnSlackAsk(signal) gegen ctx.ui.input(title,placeholder,{signal}); winner-Promise (lokal nur bei nicht-leerer Antwort), Verlierer via AbortController abgebrochen. spawnSlackAsk killt proc bei abort. classify()-Helper fuer lokal+slack (via=tui/slack im Tag, Prefix erhalten). UiInput-Typ strukturell (kein pi-Import). ui aus ctx.hasUI?ctx.ui:undefined in allen 4 Gate-Aufrufern; nextTaskGate(ui). Banner um lokale-Antwort-Hinweis ergaenzt.

pi-API belegt aus opensrc-Quellen: ExtensionContext (5. execute-Param) mit ui:ExtensionUIContext; ui.input(): Promise<string|undefined>; ExtensionUIDialogOptions.signal (Dialog-Dismiss); on(input)-Event als Alternative. Verifiziert: Orchestrator parst. Live (echte TUI-Box + Slack-Race) = manueller pio-Lauf.
<!-- SECTION:NOTES:END -->

## Final Summary

<!-- SECTION:FINAL_SUMMARY:BEGIN -->
Gate-Fragen sind jetzt parallel lokal (pi-Eingabebox) UND via Slack beantwortbar — was zuerst kommt, gewinnt; der Verlierer wird abgebrochen (Slack-Subprozess gekillt bzw. TUI-Dialog via AbortSignal geschlossen). requestHuman racet spawnSlackAsk(signal) gegen ctx.ui.input({signal}); lokale Antwort wird identisch klassifiziert (Prefix parsebar, via=tui). Headless ohne TUI -> Slack-only wie bisher. Parse + pi-API verifiziert; Live = manueller Lauf. Kein Push.
<!-- SECTION:FINAL_SUMMARY:END -->

## Definition of Done
<!-- DOD:BEGIN -->
- [ ] #1 just test passes
<!-- DOD:END -->
